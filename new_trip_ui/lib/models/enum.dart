enum BottomBarType { explore, feed, location, profile, setting }

enum LanguageType { en, de, ar }

enum ThemeModeType {
  system,
  dark,
  light,
}

enum FontFamilyType {
  montserrat,
  workSans,
  varela,
  dancingScript,
  satisfy,
  kaushanScript,
}
